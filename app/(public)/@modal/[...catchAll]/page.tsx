/**
 * بستنِ مودال با رفتن به هر آدرسِ دیگر.
 *
 * ⚠️ این فایل شبیهِ کدِ مرده است ولی نیست. رفتارِ مسیرهای موازی این است که با
 * ناوبریِ سمتِ کلاینت به آدرسی که دیگر با اسلات تطبیق ندارد، *محتوای قبلیِ اسلات
 * روی صفحه می‌ماند*. یعنی بی این فایل: مودالِ عکس باز است، کاربر روی برچسبِ دسته
 * می‌زند، صفحه‌ی دسته پشتِ سر عوض می‌شود ولی مودال همان‌جا می‌ماند. تنها راهِ
 * بستنش این است که آن آدرس‌ها هم داخلِ اسلات به چیزی تطبیق کنند که null است.
 *
 * همچنین در بارگذاریِ کاملِ «/image/<id>» همین فایل تطبیق می‌کند و null می‌دهد —
 * یعنی رهگیری اتفاق نمی‌افتد و کاربر صفحه‌ی واقعیِ عکس را می‌بیند، نه مودالی
 * روی هوا.
 */
export default function ModalCatchAll() {
  return null;
}
