/**
 * جانشینِ اسلاتِ @modal وقتی آدرسِ فعلی هیچ زیرمسیری از آن را نمی‌خواند.
 *
 * چرا لازم است (و چرا حذفش سایت را می‌شکاند): در بارگذاریِ کاملِ صفحه، نکست
 * نمی‌تواند حالتِ فعالِ اسلات‌هایی که با آدرس تطبیق ندارند را بازسازی کند. در
 * آدرسِ «/» هیچ‌چیز داخلِ @modal تطبیق نمی‌کند — حتی [...catchAll]، چون catch-all
 * غیراختیاری با صفر سگمنت جور نمی‌شود. مستندِ نکست صریح است: بی این فایل، برای
 * اسلاتِ نام‌دار خطا برمی‌گردد.
 *
 * null یعنی «هیچ مودالی باز نیست»، که حالتِ درستِ همه‌ی صفحه‌های عادی است.
 */
export default function ModalDefault() {
  return null;
}
