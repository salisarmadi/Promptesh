/**
 * مسیرهای ثابتِ پنل — در ماژولِ خودش، عمداً.
 *
 * چرا جدا از auth.ts؟ چون proxy.ts هم به همین دو مقدار نیاز دارد، و auth.ts
 * next/headers و react/cache را ایمپورت می‌کند. اگر proxy مستقیم از auth.ts
 * می‌خواند، آن دو را هم با خودش بالا می‌آورد بی‌آنکه استفاده‌ای داشته باشد.
 *
 * سودِ اصلی‌اش ولی این است: مقدارِ این مسیرها در proxy و در کدِ اپ حتماً یکی
 * است. اگر هرکدام رشته‌ی خودش را داشت، عوض‌کردنِ یکی و جا‌ماندنِ دیگری یک حلقه‌ی
 * بی‌پایانِ ریدایرکت می‌ساخت.
 */

/** صفحه‌ی ورودِ مدیر. */
export const LOGIN_PATH = "/admin/login";

/** مقصدِ پیش‌فرض بعد از ورودِ موفق. */
export const ADMIN_HOME = "/admin";
